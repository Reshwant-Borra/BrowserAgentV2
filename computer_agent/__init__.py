"""ComputerAgent production package.

M1-M3 (docs/IMPLEMENTATION_PLAN.md): grounding/freshness (`grounding.py`),
independent verification (`verification.py`), and the durable controller
slice (`journal.py`, `state.py`, `recovery.py`, `controller.py`). No policy
engine beyond a placeholder, no model integration, no real adapters yet.
"""
